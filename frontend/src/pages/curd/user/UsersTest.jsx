import React from 'react';
import { Box, Typography } from '@mui/material';

const UsersTest = () => {
    return (
        <Box p={3}>
            <Typography variant="h4">
                Users Component Test
            </Typography>
            <Typography>
                This is a test version of the Users component to check if it can be imported.
            </Typography>
        </Box>
    );
};

export default UsersTest;
